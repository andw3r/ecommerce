import ProductsList from "@/components/MainPage/ProductsList/ProductsList";
import { Box } from "@mui/material";

const Home = () => {
  return (
    <Box component="main">
      <ProductsList />
    </Box>
  );
};

export default Home;
